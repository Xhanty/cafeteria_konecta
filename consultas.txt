SELECT ID, NOMBRE, REFERENCIA, PRECIO, PESO, MAX(STOCK) AS STOCK FROM productos;
SELECT productos.NOMBRE, SUM(ventas.CANTIDAD) as CANTIDAD FROM ventas JOIN productos ON productos.ID = ventas.ID_PRODUCTO GROUP BY productos.ID ORDER BY SUM(ventas.CANTIDAD) DESC LIMIT 1;